# ACS canonical-to-checkout reconciliation

Control ID: ND-SYNC-001. Scope: this compatibility checkout; Google Drive remains canonical.

## 2026-09-15 diagnosis

The live canonical brief and Drive detail submissions contain ND-INGEST-RECOG-001 and ND-AUTO-DUR-001. GitHub origin/main contains publication 72c84d553e14a316472a7af58158957932dd1faa. This checkout remained at 9837262da0e8c6e4bb5658fad01bd6df531a655a. The Drive workspace-source-authority.json identifies the Drive ACS Source as the executable source root; Documents/General is not listed. Thus publication succeeded, while this separate workspace did not receive it. The suggested update was valid for this checkout, but does not establish a failed canonical publication.

Existing event-driven synchronization requirements cover canonical brief, source snapshot, verifier, commit and intended push. They did not establish a verified update route into every separate checkout. All three observed Codex automations are paused; the weekly assurance pilot is an assurance review, not repository synchronization. The historical-export biweekly proposal is a different workflow and is not authority to monitor export readiness. Exact internals that generated the Codex suggestion are unavailable.

Classification: M2 checkout-propagation and workspace-binding gap; intent_status=not_established. Source implementation and current verification do not establish cross-runtime operating effectiveness.

## Discretionary-method failure record

Goal: reconcile verified canonical changes while preserving user edits. Planned steps: fetch published revision, check history, fast-forward and verify. Worked: authenticated fetch found 72c84d5 and canonical records independently corroborate it; user edits were backed up to /tmp/acs-pre-sync-user-changes.patch. Failure: git merge --ff-only origin/main returned `fatal: refusing to merge unrelated histories`. Root cause: the histories are unrelated; the earlier ancestry check did not surface its exit status before the merge. Correction: use a scoped content reconciliation of the published eight-file change, without forcing unrelated histories together. Prevention: require successful ancestry status before any scheduled fast-forward; otherwise use the declared scoped mirror or report a conflict. Regression: direct ancestry check must return nonzero and the workflow must select content reconciliation, never retry the merge.

## Biweekly protocol and cost decision

Owner: Codex for active reconciliation; the native worker owns the declared mechanical copy and checks. The existing event-driven canonical publication protocol remains primary. This pilot does not replace historical export intake or the paused weekly control-assurance pilot. Before changing or renewing a schedule, inspect all local Codex automation definitions and native LaunchAgents, identify matching source/target/control scope, and update the existing owner instead of creating a duplicate.

Native job: org.neuro-div.acs.reconciliation. A daily 09:00 local macOS eligibility invocation performs work only when the persisted fourteen-day due date has arrived. RunAtLoad handles a login after a missed date. Six total due runs including the installation validation, expiry 2026-12-10, no AI calls, no new Codex tasks, bounded local reads and one resource-check process with a 90-second timeout. This is a low ordinal cost (1) against high continuity importance (3). The user's requested fourteen-day interval supplies the cadence basis; useful change probability is unmeasured and three no-op runs pause the pilot. Two errors, maximum runs, expiry or a user cost complaint also stop work. Routine no-change runs are silent.

Only two reviewed canonical control documents are copied: source-acquisition-completion-recognition-control.md and automation-cost-cadence-proportionality-control.md. The acquisition field in agent-resources.json is updated independently of other metadata. Shared executable verifiers are pinned; changed verifiers or edited target controls require active review. Original target bytes are archived before replacement, full resource checks run, and failed checks restore original bytes. Other controls, broad canonical-document integration, new files, commits and publication are active-task work, not silently claimed as covered by this pilot.

Local runtime: ~/Library/Application Support/NEURO-DIV/Reconciliation. Persisted due date and counters survive agent conversation/context loss. The separate org.neuro-div.acs.reconciliation.watchdog runs daily at 11:00 and on login, checks the worker's launchd registration and overdue status, and requests one native notification per missed due date. Both processes share this Mac, login, power, filesystem and notification permissions: they are not independently hosted liveness assurance. Notification acceptance is not proof of visible delivery. The unattended route is best-effort; active-task verified reconciliation remains primary.

Canonical runtime receipt: Neuro-DIV/03 Manifests and Data/NEURO-DIV - Repository Reconciliation - Runtime Receipt.json. Drive mounted-file read-back proves local Drive-copy existence and bytes; remote cloud replication must be verified separately. Recovery: preserve the receipt, repair the evidenced prerequisite, rerun a bounded validation, and update the same state and job; do not create another scheduler. Pause using launchctl bootout for the two named agents and set paused=true in runtime state. Renewal requires a fresh control/cost review and explicit finite bounds.

Verification performed: real scoped update in an isolated fixture; due-date suppression; failing resource check with byte restoration; conflicting user edit preserved; two-error pause and paused-run suppression. Fixture alerts used a test double, so those tests are not live notification-delivery evidence. Git ancestry regression returned 1 and selected scoped reconciliation without retrying an unrelated-history merge.

Usage preflight: existing deterministic estimator returned allow, p50/p80/p95 23/51/108.4 provider-rate credits, cost rank 2, utility ratio 66.2, medium confidence, calibration 2026-07-21, for one bounded documentation pass. This is a historical model-rate planning estimate, not a current entitlement or measured charge; the current model was not represented in its rate table. No additional LLM recurring work was enabled. Exact post-run account cost attribution is unavailable.

Canonical brief amendment ND-SYNC-001 was written and directly fetched; provider returned revision ANLCKQldI4awXi5ukVUvDrQ7c3xk_QgCNAaSRgFbFQiZPy6Dwg3ctL1jOJzldfra1i-HTyzLZlmgCl8hqfKEe9-6UWT7vSNuSrwB_4jm7w. Future execution and cross-runtime effectiveness remain open.

## Live launchd run — blocker and method-learning record

Goal: verify real scheduled work. Planned steps: register worker, run once, inspect receipt, then validate watchdog and recovery. Worked: launchd registered and executed the worker, preserved state and requested a native error notification (osascript returned 0; visible delivery unverified). Exact failure: `[Errno 1] Operation not permitted` reading the Drive source authority file from the background Python process. Active-task filesystem access succeeded. Root-cause inference: macOS background privacy access differs from the active Codex process; exact permission principal remains to be confirmed. Correction: pause immediately, preserve the failed receipt, and inspect the macOS permission boundary without another unchanged execution attempt. Prevention: no active unattended-work claim or renewal until the same launchd context reads the source, passes checks and produces a read-back receipt. The permission-regression fixture recognized a failed prerequisite, preserved originals and stopped at the configured pause boundary. Real recovery remains pending permission repair.

A read-only System Settings inspection through computer use returned `cgWindowNotFound` (server -10005). No permission was changed. Alternate path is to locate its available UI/window before any permission action; no blind repeated settings mutation is authorized by this failure.

Final installation state: both native LaunchAgents were unloaded and state was explicitly paused after the first permission error; no active schedule is claimed. Settings inventory then found System Settings and a read-only Files & Folders view, but the window became unavailable (`noWindowsAvailable`) before a specific Python permission could be verified. No privacy permission was changed. Required next action is to make the Files & Folders settings window available so the exact narrow permission can be inspected, then resolve only the confirmed boundary and revalidate this same worker.
